# Web-Deploy

Deployment of the DiscreteZoo Website. 

## Configuration

Generate a configuration file:

```
cp .env.sample .env
your-favorite-editor .env
```

## Deployment

```
docker-compose pull
docker-compose up -d
```